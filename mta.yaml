_schema-version: "3.2"
ID: productsproducts
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: productsproducts-destination-content
  type: com.sap.application.content
  requires:
  - name: productsproducts-destination-service
    parameters:
      content-target: true
  - name: productsproducts-repo-host
    parameters:
      service-key:
        name: productsproducts-repo-host-key
  - name: productsproducts-uaa
    parameters:
      service-key:
        name: productsproducts-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: productsproducts_html_repo_host
          ServiceInstanceName: productsproducts-html5-srv
          ServiceKeyName: productsproducts-repo-host-key
          sap.cloud.service: productsproducts
        - Authentication: OAuth2UserTokenExchange
          Name: productsproducts_uaa
          ServiceInstanceName: productsproducts-xsuaa-srv
          ServiceKeyName: productsproducts-uaa-key
          sap.cloud.service: productsproducts
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: productsproducts-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: productsproducts-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - productsproducts.zip
      name: productsproducts
      target-path: resources/
- name: productsproducts
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: productsproducts-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: productsproducts-destination-service
    service-plan: lite
- name: productsproducts-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: productsproducts-xsuaa-srv
    service-plan: application
- name: productsproducts-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: productsproducts-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
